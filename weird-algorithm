#include <iostream>
 
int main()
{
	long long x{};
	std::cin >> x;
 
	if (x >= 1)
	{
		std::cout << x << " ";
 
		while (x != 1 && x >= 1)
		{
			if (x % 2 == 0)
			{
				x /= 2;
				std::cout << x << " ";
 
			}
 
			else
			{
				x = (x * 3) + 1;
				std::cout << x << " ";
			}
		}
	}
}
