#include <vector>
#include <iostream>
 
typedef long long ll; 
 
int main()
{
	ll n{}; 
	std::cin >> n; 
 
	for (ll i{ 1 }; i <= n; ++i)
	{
		ll totalPos{ ((i * i) * ((i * i) - 1)) / 2 };
		ll attackPos{ 4 * (i - 1) * (i - 2) };
 
		std::cout << totalPos - attackPos << "\n";
	}
 
	return 0; 
}
